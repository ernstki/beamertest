variables:
  aspectratio: 169
  theme: Copenhagen
  # try 'horizontal' for the full set
  navigation: frame

# makes subsection titles show up; default is "discover" from doc structure
slide-level: 4
